<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.opencloudplatform.edm.dao.noticeBoardMapper">
    <insert id="insert" parameterType="noticeboard">
        insert
        into noticeboard (title, content, writer, password, register_date, update_date)
        values ( #{title}, #{content},#{writer}, #{password}, now(), now())
    </insert>
    <select id="select_all" resultType="noticeboard">
        select *
        from noticeboard
    </select>
    <select id = "selectNowPage" resultType="qnaboard">
        select idx, title, content, writer, password, register_date, update_date, view_cnt
        from qnaboard
        ORDER BY register_date DESC, idx DESC
        LIMIT #{offset}, #{pageSize}
    </select>
    <delete id="delete" parameterType="int">
        delete
        from noticeboard
        where idx = #{idx}
    </delete>
    <delete id="deleteAll">
        delete
        from noticeboard
    </delete>
    <update id="update" parameterType="noticeboard">
        update noticeboard
        set title = #{title}
        , content = #{content}
        , writer = #{writer}
        , password = #{password}
        , update_date = now()
    </update>
    <select id="totalCnt" resultType="int">
        select count(*)
        from noticeboard
    </select>
    <update id="viewCnt">
        update view_cnt
        set view_cnt = view_cnt+1
        where idx = #{idx}
    </update>
</mapper>